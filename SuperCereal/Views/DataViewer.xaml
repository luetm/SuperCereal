<UserControl x:Class="SuperCereal.Views.DataViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SuperCereal.ViewModels"
             d:DataContext="{d:DesignInstance vm:DataViewerVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Border BorderBrush="#999" BorderThickness="1">
        <TextBox AcceptsReturn="True"
                 BorderThickness="0"
                 FontFamily="Consolas Fixedsys"
                 FontSize="16"
                 FontWeight="Bold"
                 IsReadOnly="True"
                 Text="{Binding Data}" />
    </Border>
</UserControl>
